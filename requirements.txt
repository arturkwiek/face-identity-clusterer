# Core
numpy>=1.24
opencv-python>=4.8          # use opencv-python-headless on servers without GUI
onnxruntime>=1.16           # or onnxruntime-gpu with CUDA
insightface>=0.7.3
scikit-learn>=1.3
joblib>=1.3                 # persistence for the online classifier
# hdbscan>=0.8.33         # optional: needs a C++ toolchain (often fails on
                          # Windows). scikit-learn >= 1.3 ships an equivalent
                          # HDBSCAN that is used automatically when absent.
PyYAML>=6.0

# REST API + observability
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9     # multipart uploads for /detect
prometheus-client>=0.20     # optional: metrics degrade to no-ops without it

# Dev / tests
pytest>=7.0
httpx>=0.27                 # TestClient transport
